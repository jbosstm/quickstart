<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.jboss.narayana.quickstart.rts.lra</groupId>
        <artifactId>participant-availability</artifactId>
        <version>7.1.1.Final-SNAPSHOT</version>
    </parent>

    <groupId>org.jboss.narayana.quickstart.rts.lra</groupId>
    <artifactId>examples-microprofile-lra-participant-availability</artifactId>

    <name>MicroProfile LRA Participant Availability</name>
    <description>Techniques for maintaining LRA Participant availability</description>

    <properties>
        <quarkus.platform.version>3.8.6</quarkus.platform.version>
        <quarkus-plugin.version>3.8.6</quarkus-plugin.version>
        <version.surefire.plugin>2.22.2</version.surefire.plugin>
        <version.org.jboss.narayana.lra>0.0.10.Final</version.org.jboss.narayana.lra>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-bom</artifactId>
                <version>${quarkus.platform.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <repositories>
        <repository>
            <id>jboss-public-repository-group</id>
            <name>JBoss Public Maven Repository Group</name>
            <url>https://repository.jboss.org/nexus/content/groups/public/</url>
            <releases>
                <enabled>true</enabled>
            </releases>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
        <repository>
            <id>repo.eclipse.org-snapshots</id>
            <name>Eclipse MicroProfile Repository - Snapshots</name>
            <url>https://repo.eclipse.org/content/repositories/microprofile-snapshots</url>
            <snapshots>
                <enabled>true</enabled>
            </snapshots>
        </repository>
    </repositories>

    <build>
        <finalName>lra-participant-availability</finalName>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>17</source>
                    <target>17</target>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${version.surefire.plugin}</version>
                <configuration>
                    <systemPropertyVariables>
                        <java.util.logging.manager>org.jboss.logmanager.LogManager</java.util.logging.manager>
                        <maven.home>${maven.home}</maven.home>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <plugin>
                <groupId>io.quarkus</groupId>
                <artifactId>quarkus-maven-plugin</artifactId>
                <version>${quarkus-plugin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>build</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <!-- MP LRA CDI support relies on JAX-RS filters for
             starting LRAs and for enlisting resources in LRAs
        -->
        <dependency>
            <groupId>org.jboss.narayana.lra</groupId>
            <artifactId>narayana-lra</artifactId>
            <version>${version.org.jboss.narayana.lra}</version>
        </dependency>

        <!-- MP LRA CDI support defines annotations for controlling the lifecycle
             of LRAs and for controlling participation in an LRA
             -->
        <dependency>
            <groupId>org.eclipse.microprofile.lra</groupId>
            <artifactId>microprofile-lra-api</artifactId>
        </dependency>

       <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-resteasy-jackson</artifactId>
        </dependency>
        <dependency>
            <groupId>io.quarkus</groupId>
            <artifactId>quarkus-rest-client-jackson</artifactId>
        </dependency>
    </dependencies>
</project>
